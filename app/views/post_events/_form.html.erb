<%= form_for(@post_event) do |f| %>
  <% if @post_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_event.errors.count, "error") %> prohibited this post_event from being saved:</h2>

      <ul>
      <% @post_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
